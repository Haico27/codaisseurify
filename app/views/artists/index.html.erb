<h1>Artists</h1>

<% @artists.each do |artist| %>
  <p><%= link_to artist.name, artist_path(artist.id) %></p>
  <div><%= image_tag artist.photo.image.thumbnail %></div>
<% end %>

<hr>

<%= link_to "[Sort A-Z]", artists_path(@artists) %>
<%= link_to "[Sort Z-A]"%>
